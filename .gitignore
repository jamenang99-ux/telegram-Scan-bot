# ── Secrets & local data (NEVER commit) ──
.env
bot.sqlite
*.sqlite

# SQLite WAL / shared-memory (runtime PII, auto-regenerated by the bot)
bot.sqlite-wal
bot.sqlite-shm
*.sqlite-wal
*.sqlite-shm
*.sqlite-journal

# ── Dependencies ──
node_modules/

# ── Logs & temp ──
*.log
nssm-out.log
nssm-err.log
nssm_temp/

# ── Binaries ──
nssm.exe

# ── OS / editor ──
.DS_Store
Thumbs.db
